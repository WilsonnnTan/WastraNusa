import { Layout } from '@/components/auth/(login-register)/layout';
import { RegisterForm } from '@/components/auth/(login-register)/register/register-form';

export default function RegisterPage() {
  return (
    <Layout>
      <RegisterForm />
    </Layout>
  );
}
